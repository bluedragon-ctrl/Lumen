# Dependencies
node_modules/

# Logs
*.log
npm-debug.log*
logs/

# Environment / secrets
.env
.env.*
!.env.example

# Runtime / dynamic state (world snapshots, save files)
# Static authored content is committed; live dynamic state is NOT.
data/runtime/
data/save/
*.snapshot.json

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Claude Code local files (keep the shared preview launch config)
.claude/*
!.claude/launch.json
